import type { ReactNode } from 'react';

/**
 * @private
 */
// eslint-disable-next-line @typescript-eslint/naming-convention
export type _ParagraphProps = {
  title?: ReactNode;
  content?: ReactNode;
  size?: 'small' | 'large';
};
