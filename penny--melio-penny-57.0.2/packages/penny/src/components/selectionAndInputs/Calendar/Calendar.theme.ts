import { type ComponentSingleStyleConfig } from '@/theme/component-style-config-types';

export const calendarTheme: ComponentSingleStyleConfig = {
  baseStyle: {
    position: 'relative',
    marginTop: 's',
    paddingX: 's',
    paddingBottom: 's',
  },
};
