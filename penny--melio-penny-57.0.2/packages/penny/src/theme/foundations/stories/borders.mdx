import { Meta } from '@storybook/addon-docs/blocks';
import { Borders } from './utils/Borders';

<Meta title="Foundations/Borders" />

# Borders

Border tokens provide meaning and ensure visual consistency across the design system.
Below are the border tokens defined in our system. Each token includes both the border’s width and style.

Note: Make sure to set the border property before setting the borderColor property.

<Borders />
