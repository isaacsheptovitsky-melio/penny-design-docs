import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import * as BordersStories from './Borders.stories';
import { Callout } from '@/storybook-utils/Callout';
import { PageHero } from '@/storybook-utils/PageHero';
import { ShortcutTiles, ShortcutTile } from '@/storybook-utils/ShortcutTiles';
import { DocTabs, DocTab } from '@/storybook-utils/DocTabs';

<Meta of={BordersStories} />

<PageHero
  category="Foundations"
  title="Borders"
  badge="Healthy"
  description="Borders define the edges of components, surfaces, and inputs across the system."
/>

<DocTabs links={
  <ShortcutTiles>
    <ShortcutTile
      label="ZeroHeight"
      url="https://zeroheight.com/98bb1df01/v/latest/p/72d4df-borders"
    />
  </ShortcutTiles>
}>

<DocTab label="Design">

## Global tokens

The base border-weight scale. Tokens pair a width with the `solid` style.

<Canvas of={BordersStories.GlobalTokens} />

## Semantic tokens

Semantic tokens map a category to a global weight — use these in component themes.

- **`border.action.default`**, **`border.container.default`**, **`border.input.default`** → `global.25` (1px) — the standard component border.
- **`border.focus`** → `global.50` (2px) — the focus ring.

<Callout variant="note">Border weight and color are separate tokens. These define the **weight and style**; the **color** comes from a color token like `semantic.border.static` or `semantic.border.brand`.</Callout>

## Selected state

To indicate a selected element (a chosen option, an active card), keep the `global.25` weight but swap the border color from `semantic.border.static` to **`semantic.border.brand`** — the brand color signals selection.

<Canvas of={BordersStories.Selected} />

</DocTab>

<DocTab label="Development">

</DocTab>

<DocTab label="ADA">

</DocTab>

</DocTabs>
